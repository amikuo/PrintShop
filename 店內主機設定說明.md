# PrintShop 店內主機設定

## 一鍵啟動

雙擊 `Start_PrintShop.bat`。工具會先確認 5000 連接埠是否已有 PrintShop 執行中；若沒有，才啟動伺服器，然後開啟首頁。

伺服器命令視窗必須保持開啟。關閉該視窗即停止店內其他設備連線。

## 登入 Windows 後自動啟動

雙擊 `Enable_AutoStart.bat` 一次。之後這個 Windows 使用者每次登入時，PrintShop 都會自動啟動。

若要取消，雙擊 `Disable_AutoStart.bat`。

## Windows 防火牆

確認目前網路被 Windows 設為「私人網路」，再雙擊 `Open_Firewall_5000.bat`。Windows 顯示權限確認時選「是」。工具只開放私人網路的 TCP 5000，不對公用網路開放。

## 固定店內 IP（建議使用路由器保留位址）

1. 雙擊 `Show_LAN_Info.bat`，記下 IPv4、Gateway 與 MAC。
2. 用瀏覽器開啟 Gateway 顯示的路由器管理位址。
3. 在路由器尋找「DHCP 保留」、「位址保留」、「固定租約」或 `Address Reservation`。
4. 以店內主機的 MAC 保留目前 IPv4。例如目前若是 `192.168.1.127`，就保留該位址給這台主機。
5. 儲存設定，重新連線網路或重開主機，再執行 `Show_LAN_Info.bat` 確認 IPv4 沒有改變。

路由器保留位址能避免與其他設備撞 IP。若路由器沒有此功能，不要直接猜測 Windows 靜態 IP；應先確認路由器的 DHCP 範圍、閘道與 DNS。
